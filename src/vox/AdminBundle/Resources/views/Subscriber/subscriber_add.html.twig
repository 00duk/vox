{% extends "voxAdminBundle:Admin:main.html.twig" %}

{% block title %}Add Subscriber{% endblock %}
{% block content_title %}<h1>Add Subscriber</h1>{% endblock %}

{% block content %}

{#{{ dump(listReleases) }}#}


    {{ form_start(form) }}


    <div class="form-group">
        <label>Email: </label>
        {{ form_widget(form.email) }}
    </div>

    <div class="form-group">
        <label>Enable:
            {{ form_widget(form.enable) }}
        </label>
    </div>

    {{ form_widget(form.save) }}
    {{ form_end(form) }}
    {{ tinymce_init() }}





{% endblock %}